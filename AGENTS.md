# Agent 学习仓库协作规则

## 1. 仓库目标

这个仓库用于建立可迁移、可验证、面试可输出的 Agent 工程心智。

学习内容必须回答：

1. 为什么需要这个机制。
2. 它在真实系统中怎样工作、怎样失败。
3. 它与前后知识怎样形成因果链。
4. 面试中怎样从核心判断回答到工程证据。

## 2. 文档职责

- `docs/学习总纲.md`是唯一知识边界，负责完整知识图和掌握标准。
- `docs/学习进度.md`只记录状态、薄弱点和证据。
- `docs/课程/`保存按当前进度动态生成的 CU，不维护固定课表。
- 每个 CU 包含`推导式理解.md`和`面试训练.md`两条轨道。
- `docs/面试题库/`只保存真实问题、追问和来源，不独立定义知识。
- `docs/资料索引.md`负责来源、证据等级和待验证方向。
- `README.md`只负责导航，`docs/adr/`只记录架构决策。

同一问题只能有一份权威内容。课程、题库、进度和资料通过链接引用学习总纲，不重新建立知识树。

## 3. 动态课程

不得预先锁死固定课数、课程分组和完整学习顺序。

每次开始或继续学习时，根据下面的信息动态编译当前 CU：

```text
学习目标
  +
学习总纲中的知识依赖
  +
学习进度与薄弱点
  +
项目或面试需求
```

一个 CU 可以覆盖多个知识点，但必须共享一个中心问题、贯穿案例和连续状态。课程编号只表示实际生成顺序，不表示稳定知识顺序。

## 4. 推导式理解

第一轨必须使用一个贯穿项目或案例，让同一个系统逐步演进：

```text
已有能力
  ↓
暴露问题
  ↓
现有机制为什么不够
  ↓
引入新机制
  ↓
状态、控制权或数据发生变化
  ↓
留下下一问题
```

正文先给完整推导链和案例初始状态，再按问题演进展开。知识点不能写成互不相干的百科章节。

至少包含：

1. 本次选课依据和覆盖知识点。
2. 核心推导链。
3. 贯穿案例的初始状态和版本演进。
4. 真实输入、状态、控制权、输出和副作用。
5. 正常链路与失败链路。
6. 最小协议、实现或实验设计。
7. 边界、代价和下一阶段问题。
8. 自测问题和知识图谱。
9. 面试题索引，但不展开最终答案。

## 5. 面试训练

第二轨必须在推导式理解稳定后生成。

每道题至少包含：

1. 完整题目与题型。
2. 一句话核心判断。
3. 启发式思考：没有它会怎样、怎样解决、有什么边界。
4. 30 秒直接回答。
5. 完整回答。
6. 必要的 ASCII 图。
7. 原理、工程和项目追问。
8. 实验、Trace、代码或真实请求证据。
9. 对应推导式理解章节。

题目版不能另造结论。更换问法后仍应能从同一机制推导答案。

## 6. 授课节奏

- 默认先完整形成推导式理解，再进入面试训练。
- 用户要求互动时，一次只推进推导链中的一个问题，等待回答后继续。
- 用户要求连续讲解时，思考题用于制造认知冲突，随后直接推导。
- 生成文档不代表掌握，只有真实复述、实验和复核才能更新进度。

## 7. 表达规则

- 默认使用中文。
- 较难英文第一次出现时写成`英文(精简通俗注释)`。
- 先给最简心智和图，再给代码与框架。
- 框架只作为机制样本，不按品牌组织知识。
- 不编造项目数据、线上规模、源码结论或已完成事实。
- 不为了形式完整让每个章节机械复制同一模板。

## 8. 质量门禁

新增或修改内容后至少检查：

1. 是否能回到学习总纲中的唯一主归属。
2. CU 是否由当前进度和目标动态选择。
3. 删除任一中间章节后，后续推导是否会断裂。
4. 贯穿案例的用户目标、输入、状态和工具是否保持连续。
5. 推导式理解与面试训练是否结论一致。
6. 是否包含真实机制、失败边界和验证方法。
7. 是否存在失效链接、绝对用户路径或未解释英文。
